# Convex — set by `npx convex dev` (writes .env.local automatically).
# The public URL the browser client connects to.
NEXT_PUBLIC_CONVEX_URL=

# Resend (M1 — magic-link auth). Placeholder here; the real key is set as a
# Convex environment variable (`npx convex env set`), never committed.
# RESEND_API_KEY=
