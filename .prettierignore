# Generated / build output — mirror eslint.config.mjs globalIgnores.
convex/_generated/
.next/
out/
build/
next-env.d.ts

# Machine-owned / vendored (regenerated by tooling — don't reflow).
package-lock.json
skills-lock.json
.agents/

# Prose & throwaway artifacts — Prettier owns code layout, not prose.
# (Its Markdown reflow, e.g. *emphasis* -> _emphasis_, is pure churn here.)
docs/
*.md
